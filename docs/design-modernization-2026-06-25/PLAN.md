"# DHM Guide — 2026 Modernization Master Plan\n\n## 1. What \"Modern\" Means Here\n\nThe site reads as a *supplement-affiliate landing page that happens to cite studies.* The 2026 bet is to flip that into *an editorial science desk that happens to sell.* Every page carries the same four dated tells and the same strategic flaw:\n\n| Dated tell (all 6 pages) | The fix |\n|---|---|\n| Gradient clip-text headings (2021) | Solid `--color-ink`, one optional brand-accent word |\n| Green→blue page wash | Warm paper foundation `#FAF8F4` |\n| Georgia browser-default headings | Editorial type (Fraunces + Inter) — *fenced into its own late test* |\n| Shadow-on-every-card monotony | Border-first elevation (depth signals hierarchy) |\n\n**The strategic inversion:** green is doing *fill* duty, which dilutes the one color that should own conversion. We demote green to a selective brand/trust accent and make **orange the only saturated color on the page** so `Check Price on Amazon` becomes the unmissable focal point. Calming the field *is* a salience mechanism — this is the rare modernization that should **hold or raise** affiliate CTR, not risk it.\n\n## 2. Cross-Cutting Foundations (ship with/before page variants)\n\n1. **Color tokens** — add NEW `@theme` tokens in `src/App.css` (verified location, line 20): `--color-paper #FAF8F4`, `--color-surface #FFFFFF`, `--color-ink #1A1D1A`, `--color-ink-soft #4A4E48`, `--color-brand #15803d`, `--color-brand-soft #ECF3EE`, `--color-cta #F97316` / `--color-cta-hover #EA580C`, `--color-border #E7E3DB`. **Additive only** — never renumber existing tokens (Pattern #16), never use `tailwind.config.js` (v4 ignores it, Pattern #15). Smoke-test dist CSS.\n2. **Flatten gradient headings** — common to all six variants; no font, no CWV risk.\n3. **Border-first elevation** — tier 0 flat-on-paper + 1px border / tier 1 interactive cards get a tuned soft shadow / tier 2 modals + sticky bars only.\n4. **Orange discipline** — in *foundation* tests the orange CTA is byte-identical to control. Orange only *changes* in Guide (green→orange lever) and *appears net-new* in Research + Never-Hungover (gap-fill).\n5. **Motion hygiene** — delete homepage parallax (also fixes mobile broken-image bug), remove blog-hub pulse dot, gate Framer behind `prefers-reduced-motion` + `viewport once`, drop all count-ups from v1.\n6. **Test infra (build once, reuse 6×)** — thin wrapper reads `useExperiment(key)` → renders untouched `Page.jsx` until `isLoading` resolves → swaps to `Page.modern.jsx`. Verified: `useExperiment` returns `{variant, payload, isLoading}`; `trackAffiliateClick` accepts `experimentKey`/`variant`. No new framework.\n7. **Typography (the #1 lever, fenced LAST)** — self-hosted subset variable Fraunces + Inter, `font-display:optional`, size-adjust metric matching, zero-CLS, <140KB. Never bundled with a foundation/CTA test.\n\n## 3. Ranked A/B Roadmap\n\nSequenced by expected-lift × conversion-proximity × traffic, leading with the cleanest, highest-traffic tests. All variants gated on `isLoading`; control byte-untouched.\n\n| # | Page | experimentKey | Lever | Effort | Expected lift | Orange CTA |\n|---|---|---|---|---|---|---|\n| 1 | Reviews | `reviews-modern-foundation-v1` | Paper + ink headings + de-orange table (winner-only) | M | 0 to +8% (hold/lift) | **identical to control** |\n| 2 | Homepage | `homepage-editorial-refresh-v1` | Paper + flatten 4 gradient headings + recolor bands + delete parallax | M | 0 to +6% | **identical to control** |\n| 3 | Guide | `guide-editorial-modern-v1` | Paper + de-rainbow + **green→orange CTAs** + mobile sticky orange bar | M | +7 to +15% | **CHANGES (the lever)** |\n| 4 | Research | `research-editorial-cta-v1` | Re-skin + **ONE net-new body orange CTA** at proof moment | M | Meaningful (fills a gap) | **net-new** |\n| 5 | Never-Hungover | `never-hungover-editorial-hub-v1` | Paper + featured hero + **net-new desktop orange product rail** | M | Modest, pure addition | **net-new (desktop)** |\n| 6 | Compare | `compare-calm-foundation-v1` | Paper + de-noise table + re-skin winners band | M | 0 to +8% (hold/lift) | **identical to control** |\n| 7 | Site-wide | `editorial-typography-v1` | Self-hosted Fraunces + Inter (zero-CLS) | L | Uncertain (perceived quality) | unchanged |\n\n### Why this order\n- **Reviews + Homepage first** — highest-traffic, most conversion-proximate, cleanest hold-or-lift hypothesis. Reviews lands the tokens + wrapper that pages 2–6 reuse.\n- **Guide third** — the one foundation variant where orange *changes*; that's the lever (Pattern #12 saw +21% from exactly this green→orange swap) and the page is the documented biggest funnel leak.\n- **Research + Never-Hungover** — the only two pages with a *verified conversion gap* (no body/desktop affiliate path). Their variants *add* clicks rather than redistribute.\n- **Compare sixth** — same class as Reviews/Home but lowest traffic of the conversion pages.\n- **Typography last** — biggest lever but biggest CWV risk on a zero-web-font, SEO-sensitive site. Isolated, shipped only after foundations win.\n\n## 4. Quick Wins (site-wide, low risk)\n- Flatten the gradient H1 to solid ink on all six pages (pure class swap, no font).\n- Add the new `@theme` color tokens once; verify in dist CSS.\n- Delete the homepage parallax (removes dated effect + fixes mobile broken-image bug).\n- Strip decorative emoji from headings/badges/labels.\n- Unicode stars → Lucide Star fills; hollow-red-circle cons → Lucide minus.\n- Remove the blog-hub `animate-pulse` stat dot.\n- Border-first card elevation replacing shadow-on-everything.\n\n## 5. Risks & De-Risking\n- **Web-font CWV** → fence into the final isolated test; one pair, self-hosted+subset, `font-display:optional`, size-adjust, <140KB; measure LCP/CLS; system-stack fallback.\n- **Attribution confounding** → one lever class per v1; instrument per-row/per-component `affiliate_link_click`, not just total.\n- **Shared `InlineComparisonTable` contamination** → gate de-orange to the Reviews placement via a prop (verified shared across Reviews.jsx + NewBlogPost.jsx).\n- **Tailwind v4 silent token regression** → tokens in `@theme` only; smoke-test built CSS.\n- **Z-index/stacking traps (Patterns #14–16)** → no transform/opacity/filter on header/main/section ancestors; reuse existing z-tokens; new blog-hub rail stays in-flow.\n- **Flicker** → gate every swap on `isLoading`.\n- **SEO** → identical H1/H2 text, hierarchy, links, citations, schema; same `useSEO` call; don't virtualize the blog grid (~160 crawlable links).\n- **Double-counting** → wire `trackAffiliateClick({experimentKey, variant})` only; the global listener already fires.\n- **Mount/animate CLS** → reserve min-height on the new Research CTA block; drop all count-ups from v1.\n\n## 6. Estimates note (per CLAUDE.md)\nEvery variant is M effort: each is class-only or reuse-existing (paper tokens, flattened headings, border swaps, an existing sticky-bar pattern, an already-wired `trackAffiliateClick`). Most individual changes are <20 lines. Lift estimates divide the designer hype by ~3 — foundation tests are honestly **hold-or-modest-lift**, the value being trust + de-dating the highest-traffic pages; the genuine CTR upside concentrates in the three CTA-touching variants (Guide recolor, Research + Never-Hungover gap-fills)."

---

## Appendix: structured A/B roadmap

| Rank | Page | Experiment key | Effort | Expected lift | Primary metric | Hypothesis |
|---|---|---|---|---|---|---|
| 1 | Best DHM Supplements (Reviews) | `reviews-modern-foundation-v1` | M | Modest. Hold-or-slight-lift on total affiliate CTR (realistic 0 to +8%); honest downside is click redistribution toward the winner row. Real upside is scroll depth + perceived credibility. | affiliate_link_click | A warm-paper neutral foundation that concentrates the single orange accent on the recommended product (de-oranging the comparison table so only the winner row keeps a filled orange button) raises perceived credibility and CTA salience, holding or lifting affiliate CTR while improving scroll depth. |
| 2 | Homepage | `homepage-editorial-refresh-v1` | M | Modest. Hold-or-slight-lift (realistic 0 to +6%). Primary win is removing the dominant 'dated' signal on the most-trafficked entry page; parallax deletion also recovers a broken-image trust hit on mobile. | affiliate_link_click | A credible editorial frame (warm paper, flattened gradient headings, recolored off-palette gradient bands, monochrome stats, border-first cards, parallax removed) holds or raises affiliate CTR because the calm field makes the untouched orange CTAs the obvious focal point. |
| 3 | DHM Hangover Relief Guide | `guide-editorial-modern-v1` | M | Higher-confidence than ranks 1-2 because this CHANGES the CTA color (proven lever) rather than holding it. Honest: +7 to +15% (Pattern #12's +21% was a different surface; divide hype). This page is the documented biggest funnel leak, so even a modest lift is high-value. | affiliate_link_click | Recoloring every primary CTA from green to the single protected orange accent (Pattern #12 saw +21% from exactly this swap) ON a calm paper foundation that makes the lone orange button unmissable lifts affiliate clicks — the conversion lever and the trust lever reinforce each other. |
| 4 | The Science Behind DHM | `research-editorial-cta-v1` | M | Potentially the highest absolute lift of the set because it fills a verified GAP (zero body CTA today) rather than redistributing — but from a smaller, lower-traffic audience. Honest: meaningful relative lift in affiliate_link_click from this page's converted-reader segment; modest in absolute site terms. | affiliate_link_click | Inserting ONE orange 'Check Price on Amazon' CTA right after the Key Findings / RCT proof sections — where reader conviction peaks — on a calm-neutral re-skin captures a high-intent audience that currently has NO mid-body affiliate path, directly adding affiliate clicks. |
| 5 | Never Hungover Blog Hub | `never-hungover-editorial-hub-v1` | M | Net-new desktop affiliate path = direct upside from a surface that converts nothing today; honest absolute lift is modest because the hub is top-of-funnel, but it is pure addition, not redistribution. | affiliate_link_click (rate per visitor); secondary: article click-through, scroll depth, time-on-page | Adding a single compact orange 'Check Price on Amazon' product rail to the DESKTOP hub — which today has NO CTA at all (StickyMobileCTA is md:hidden) — plus a featured hero card and warm-paper re-skin, gives desktop readers a direct affiliate path and breaks the 169-card monotony, raising affiliate clicks per visitor. |
| 6 | Compare DHM Supplements | `compare-calm-foundation-v1` | M | Modest, same class as Reviews/Home/Compare foundation tests (realistic 0 to +8% hold-or-lift). Lowest traffic of the conversion pages, hence last. | affiliate_link_click | Making orange the ONLY saturated color on the page — warm paper bg, de-noised single-rubric comparison table with a green-soft winner pill, re-skinned Category Winners band, border-first cards — raises affiliate CTR by giving the orange CTAs uncontested salience. |
| 7 | Site-wide (all six) | `editorial-typography-v1` | L | Uncertain. Typography is the biggest stated lever but the hardest to attribute and the biggest CWV risk on a zero-web-font, SEO-sensitive site. Treat as a perceived-quality bet with a CWV guardrail, not a CTR play. | affiliate_link_click (guardrail metrics: LCP, CLS) | Self-hosted Fraunces display + Inter text (replacing Georgia/system) reads as 'editorial/premium/credible' and is the #1 modernization lever — IF it ships with zero CLS and no LCP regression, it holds or lifts affiliate CTR via perceived quality. |